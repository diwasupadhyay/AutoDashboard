services:
  backend:
    build: ./backend
    container_name: autodashboard-api
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: autodashboard-ui
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
